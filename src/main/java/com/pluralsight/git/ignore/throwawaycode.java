package com.pluralsight.git.ignore;

import com.pluralsight.Vehicle;

public class throwawaycode {
//    public void getVehiclesByPrice(int min, int max) {
//        for(Vehicle v : inventory) {
//            if(v.getPrice() >= min && v.getPrice() <= max) {
//                System.out.println("Vehicle found at: " + name);
//                System.out.printf("vin: %d, year: %d, make: %s, model: %s, vehicleType: %s, color: %s, odometer: %d, price: .%2f", v.getVin(), v.getYear(), v.getMake(), v.getModel(), v.getVehicleType(), v.getColor(), v.getOdometer(), v.getPrice());
//            } else {
//                System.out.println("No vehicles found in this price range.");
//            }
//        }
//    }
//
//    public void getVehiclesByMakeModel(String make, String model) {
//        for(Vehicle v : inventory) {
//            if(v.getMake().equalsIgnoreCase(make) && v.getModel().equalsIgnoreCase(model)) {
//                System.out.println("Vehicle found at: " + name);
//                System.out.printf("vin: %d, year: %d, make: %s, model: %s, vehicleType: %s, color: %s, odometer: %d, price: .%2f", v.getVin(), v.getYear(), v.getMake(), v.getModel(), v.getVehicleType(), v.getColor(), v.getOdometer(), v.getPrice());
//            } else {
//                System.out.println("No vehicles found with this make and model.");
//            }
//        }
//    }
//
//    public void getVehiclesByYear(int min, int max) {
//        for(Vehicle v : inventory) {
//            if(v.getYear() >= min && v.getYear() <= max) {
//                System.out.println("Vehicle found at: " + name);
//                System.out.printf("vin: %d, year: %d, make: %s, model: %s, vehicleType: %s, color: %s, odometer: %d, price: .%2f", v.getVin(), v.getYear(), v.getMake(), v.getModel(), v.getVehicleType(), v.getColor(), v.getOdometer(), v.getPrice());
//            } else {
//                System.out.println("No vehicles found in this year range.");
//            }
//        }
//    }
//
//    public void getVehiclesByColor(String color) {
//        for(Vehicle v : inventory) {
//            if(v.getColor().equalsIgnoreCase(color)) {
//                System.out.println("Vehicle found at: " + name);
//                System.out.printf("vin: %d, year: %d, make: %s, model: %s, vehicleType: %s, color: %s, odometer: %d, price: .%2f", v.getVin(), v.getYear(), v.getMake(), v.getModel(), v.getVehicleType(), v.getColor(), v.getOdometer(), v.getPrice());
//            } else {
//                System.out.println("No vehicles found with this color.");
//            }
//        }
//    }
//
//    public void getVehiclesByMileage(int min, int max) {
//        for(Vehicle v : inventory) {
//            if(v.getOdometer() >= min && v.getOdometer() <= max) {
//                System.out.println("Vehicle found at: " + name);
//                System.out.printf("vin: %d, year: %d, make: %s, model: %s, vehicleType: %s, color: %s, odometer: %d, price: .%2f", v.getVin(), v.getYear(), v.getMake(), v.getModel(), v.getVehicleType(), v.getColor(), v.getOdometer(), v.getPrice());
//            } else {
//                System.out.println("No vehicles found within this mileage range.");
//            }
//        }
//    }
//
//    public void getVehiclesByType(String type) {
//        for(Vehicle v : inventory) {
//            if(v.getColor().equalsIgnoreCase(type)) {
//                System.out.println("Vehicle found at: " + name);
//                System.out.printf("vin: %d, year: %d, make: %s, model: %s, vehicleType: %s, color: %s, odometer: %d, price: .%2f", v.getVin(), v.getYear(), v.getMake(), v.getModel(), v.getVehicleType(), v.getColor(), v.getOdometer(), v.getPrice());
//            } else {
//                System.out.println("No vehicles with this type found..");
//            }
//        }
//    }
//
//    public void getAllVehicles() {
//        for(Vehicle v : inventory) {
//            System.out.println("Dealership: " + name);
//            System.out.printf("vin: %d, year: %d, make: %s, model: %s, vehicleType: %s, color: %s, odometer: %d, price: .%2f", v.getVin(), v.getYear(), v.getMake(), v.getModel(), v.getVehicleType(), v.getColor(), v.getOdometer(), v.getPrice());
//        }
//    }
}
